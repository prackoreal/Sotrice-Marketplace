{
  "version": "0.1.9",
  "notes": "Toolbox and Marketplace now correctly group the math plugin (and any future package) as one block instead of showing its members separately or picking one as the group's head. Also the first release with a real, signature-verified in-app auto-updater -- check Settings for manual/automatic update mode."
}
