{
  "version": "0.1.20",
  "notes": "Math classroom Stage 2 (student profiles, a second non-kiosk join entry path, teacher leave-lock, performance auto-grouping), fixes for multi-class bots piling into one class and ungrouping silently disabling a plugin, presentation-mode/F11/roster-overflow fixes, and a real fix to the in-app updater itself (the sidecar process wasn't confirmed dead before being overwritten, verified end to end this time by actually clicking Update, not just doing a fresh install)."
}
