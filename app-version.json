{
  "version": "0.1.18",
  "notes": "Real cross-device networking for the math classroom (LAN binding, a class-code access gate, LAN-IP display, real host/port/code join flow), independent multi-instance support, kiosk-mode student lock + teacher presentation mode + per-class persistence, and a Stage 1 content rebuild: free-response questions, hints, and a real question-authoring UI in the teacher dashboard. Also fixes a permanent \"waiting for the class session to start\" hang caused by a startup race in the frontend's attribute-push handling."
}
