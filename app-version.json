{
  "version": "0.1.7",
  "notes": "Fixes 'Avoid rain' only ever affecting city-residents — city-routing now honors the same toggle instead of always routing around bad weather regardless of it. Fixes picking up a grouped spin visually teleporting to a stale position instead of starting where it actually was. The minimap now shows whichever view (2D/3D) the main view ISN'T currently in, instead of mirroring the same one."
}
