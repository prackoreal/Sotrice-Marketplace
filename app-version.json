{
  "version": "0.1.15",
  "notes": "Fixes the real cause behind Sotrice repeatedly failing to reopen after an update or crash: a leftover background server process from the previous session would keep holding the connection ports, leaving nothing for the fresh launch to actually talk to. Every launch now checks for and clears away that kind of leftover before starting its own, no matter how the previous session ended -- reproduced the exact stuck scenario on purpose and confirmed it now recovers cleanly."
}
