{
  "version": "0.1.23",
  "notes": "A crashed teacher process is now detectable instead of the dashboard silently pretending everything's fine. Existing installs are now actually pruned of preinstalled spins. Real math notation (KaTeX) now renders in question text, options, and hints. macOS build also available (Apple Silicon + Intel universal, smoke-tested on real hardware)."
}
