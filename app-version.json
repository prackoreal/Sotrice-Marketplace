{
  "version": "0.1.14",
  "notes": "The connected/disconnected indicator now lives only in Settings (Connection section), not the top bar. The bottom Process Log bar can fully hide now, not just collapse to a thin strip. Fixes the group-drag ghost preview for a hand-built group of unrelated plugins, not just ones sharing a Marketplace package. No more blank white flash when the app launches."
}
