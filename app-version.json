{
  "version": "0.1.19",
  "notes": "Fixes a broken 0.1.18 release: the installer was missing WebView2Loader.dll, a Windows system component the app needs just to start -- it failed to launch at all right after installing. Rebuilt with the correct build target (--target x86_64-pc-windows-gnu) and verified the DLL is genuinely present inside the installer this time before publishing, instead of just trusting a green build."
}
