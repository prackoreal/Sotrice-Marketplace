{
  "version": "0.1.17",
  "notes": "Fixes Sotrice appearing to open but showing nothing on screen. The window was actually rendering at 5x5 pixels instead of maximized, confirmed directly via Windows APIs on a real affected machine -- not a crash, a startup regression from the 0.1.14 fix meant to avoid a blank flash on launch. Reverted outright rather than patched: an invisible-but-running app is a far worse failure than a brief flash."
}
