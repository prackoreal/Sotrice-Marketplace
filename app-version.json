{
  "version": "0.1.13",
  "notes": "Fixes an update-install deadlock that could leave Sotrice stuck disconnected on \"Updating...\" forever. App updates and plugin auto-update now recheck every 30 minutes while the app is open, not just at launch. Fixes the group-drag preview showing separated plugins mid-drag instead of the collapsed group. Supports snapping the window to half the screen. Explains what the Files panel is for."
}
